import template from './video-player.component.html';
import './video-player.component.scss';

const VideoPlayerComponent = {
  template
};

angular.module('app.components').component('videoPlayerComponent', VideoPlayerComponent);
